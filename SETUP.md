# Setup

- Postgres, Redis required
- Install Python dependencies from `requirements.txt`
- Install [tnoodle-cli](https://github.com/SpeedcuberOSS/tnoodle-cli/releases) and configure PATH
- Start ```services.sh```
- Start Django server